#include <stdio.h>
void main()
{
    int firstNumber = 50; // declaration + initialization
    int secondNumber = 60;

    int sum = firstNumber + secondNumber;

    printf("SUM Of %d & %d is %d", firstNumber, secondNumber, sum);
}
